export const ENV_CONFIG = {
  // App configuration
  APP_NAME: 'Dagster+ Mobile',
  VERSION: '1.0.0',
  
  // Default values (users will configure their own)
  DEFAULT_DAGSTER_URL: 'https://your-dagster-instance.cloud/graphql',
  DEFAULT_WORKSPACE: 'prod',
}; 