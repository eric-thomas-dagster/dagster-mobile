/**
 * Predefined insight API endpoints
 * These are relative paths that will be appended to the base URL
 * Based on Dagster+ insights page structure
 */
export const INSIGHTS_ENDPOINTS = [
  // Main insights endpoint - returns all insights data
  '/insights',
  // Alternative: individual endpoints if needed
  // '/api/insights/metrics',
  // '/api/insights/hourly',
  // '/api/insights/assets',
];

